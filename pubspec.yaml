name: city_alert
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  emailjs: ^4.0.0
  url_launcher: ^6.3.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  supabase_flutter: ^2.12.2
  image_picker: ^1.1.2

  geolocator: ^11.0.0
  geocoding: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/arrierplant.jpg
    - assets/logo.jpg